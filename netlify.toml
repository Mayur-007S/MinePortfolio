[build]
  command = "npm run build"
  publish = "dist"

# Force HTTPS redirect across all endpoints
[[redirects]]
  from = "http://*"
  to = "https://:splat"
  status = 301
  force = true

# SPA fallback for React Router
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
